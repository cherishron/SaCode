# SaCode TUI 重构优化方案

## 现状问题

### 1. 文件过大
- `tui/mod.rs` 超过 **8347 行**
- 渲染逻辑、业务逻辑、事件处理全部混在一起
- 每次修改都要重新编译整个文件，编译时间长
- 维护困难，定位问题慢

### 2. 消息渲染无缓存
```rust
fn render_message_lines(app: &App) -> Vec<RenderedMessageLine> {
    // 每次渲染都重新计算所有消息
    // 消息越多越慢
}
```
- 每条消息每次都重新计算样式、换行、着色
- 消息多的时候（几百条）渲染卡顿明显

### 3. 布局计算过于动态
```rust
let input_line_count = if input_is_editable && !app.input.is_empty() {
    let (lines, _, _) = layout_input_lines(&self.input, input_inner_width);
    lines.len().max(1)
} else {
    1
};
let input_height = (input_line_count as u16 + 2).clamp(3, 8);
```
- 输入框高度每次渲染都重新计算
- 消息区域高度跟着输入框变化，连锁反应

### 4. 滚动没有虚拟化
- 所有消息都渲染，不管是否在可视区域
- 滚动时全量重绘，帧率低

---

## 目标

1. **提升渲染性能**：消息多时也能流畅滚动
2. **提升编译速度**：模块化拆分，增量编译
3. **提升代码可维护性**：职责分离，便于后续扩展
4. **收敛交互复杂度**：统一状态机、队列运行时和异步收尾逻辑

---

## 方案设计

### Phase 0：状态收敛（P0）

#### 当前复杂度中心

当前 TUI 的主要复杂度已经不只在渲染，还集中在以下几类状态与流程：

1. `App` 同时承载聊天、队列、会话、配置、选择器、审批、等待回答、统计和渲染视口
2. `InputMode` 同时表示输入组件状态和业务交互状态
3. `enqueue_or_start_message` -> `start_queued_message` -> `spawn_chat_task` -> `poll_async_results` 构成完整运行时链路
4. `pending_question` / `approval` / `todo confirm` 都属于“等待用户继续”的同类交互

如果直接先拆文件，会把复杂度分散到多个文件，后续仍然难维护。

#### 优先收敛的对象

建议先新增状态层和运行时层：

```text
tui/
├── state.rs           # QueueState / InteractionSession / SelectionState
├── task_runtime.rs    # 队列执行、启动、取消、收尾
└── interaction.rs     # pending question / approval / todo confirm
```

#### 第一阶段目标

1. 从 `App` 中抽出：
   - `QueueState`
   - `InteractionSession`
   - `SelectionState`
2. 让 `finish_active_task()`、队列续跑、取消当前任务、等待用户回答恢复继续执行，都通过统一运行时入口处理
3. 让 `InteractionState` 不再只靠 `InputMode` 间接表达

#### 建议结构

```rust
pub struct QueueState {
    processing: bool,
    busy_message: String,
    active_task_id: Option<u64>,
    queued_messages: VecDeque<QueuedMessage>,
    active_child: Option<Arc<Mutex<Child>>>,
}

pub enum InteractionSession {
    None,
    TodoConfirm,
    Question(PendingQuestionState),
    Approval(PendingApprovalState),
}

pub struct SelectionState {
    provider_options: Vec<String>,
    model_options: Vec<ModelOptionEntry>,
    theme_options: Vec<String>,
    session_options: Vec<SessionInfo>,
}
```

#### 收益

1. 后续模块拆分有稳定边界
2. `Esc` / `Enter` / 等待回答 / 审批 / 队列续跑 的行为更一致
3. 为后续单元测试和回归测试创造清晰入口

---

### Phase 1：文件拆分（P1）

#### 目录结构

```
tui/
├── mod.rs              # 入口，只保留主循环和模块声明
├── app.rs              # App 聚合根和业务入口
├── state.rs            # 状态模型
├── task_runtime.rs     # 队列执行与异步任务运行时
├── interaction.rs      # 等待回答 / 审批 / todo 确认
├── theme.rs            # ThemePalette 定义
├── event.rs            # 事件处理（按键、鼠标、粘贴）
├── render.rs           # 渲染入口，ui() 函数
└── render/
    ├── mod.rs          # 渲染分发
    ├── message.rs      # 消息列表渲染（核心优化点）
    ├── input.rs        # 输入框渲染
    ├── header.rs       # 顶部状态栏
    ├── sidebar.rs       # 侧边栏（Todo/Task/Git）
    ├── modal.rs        # 弹窗渲染
    └── selector.rs     # 各种选择器（Provider/Model/Theme等）
```

#### 各文件职责

| 文件 | 行数预估 | 职责 |
|------|---------|------|
| `tui/mod.rs` | ~100 行 | `run_tui()` 主循环，模块导入 |
| `tui/app.rs` | ~500-800 行 | `App` 聚合根，业务入口方法 |
| `tui/state.rs` | ~200-300 行 | `QueueState`、`InteractionSession`、选择状态 |
| `tui/task_runtime.rs` | ~200-300 行 | 队列启动、取消、收尾、续跑 |
| `tui/interaction.rs` | ~200-300 行 | pending question / approval / todo confirm |
| `tui/theme.rs` | ~100 行 | `ThemePalette` 定义，主题切换 |
| `tui/event.rs` | ~500 行 | 按键/鼠标/粘贴事件处理 |
| `tui/render.rs` | ~50 行 | `ui()` 入口，区域划分 |
| `tui/render/message.rs` | ~300 行 | 消息列表渲染，**缓存 + 可见行裁剪** |
| `tui/render/input.rs` | ~200 行 | 输入框渲染，高度缓存与输入行缓存 |
| `tui/render/header.rs` | ~100 行 | 顶部状态栏 |
| `tui/render/sidebar.rs` | ~200 行 | 侧边栏 |
| `tui/render/modal.rs` | ~150 行 | 弹窗基座 |
| `tui/render/selector.rs` | ~400 行 | 各种选择器 |

---

### Phase 2：消息缓存（P2）

#### 核心思路

消息内容不变时，缓存渲染好的行，避免重复计算。

```rust
// tui/app.rs
pub struct App {
    // 现有字段...
    
    // 新增：缓存渲染好的消息行
    cached_message_lines: Vec<RenderedMessageLine>,
    cache_valid: bool,
    
}

impl App {
    /// 推送新消息后失效缓存
    fn push_message(&mut self, message: Message) {
        self.messages.push(message);
        self.invalidate_cache();
    }
    
    /// 失效缓存
    fn invalidate_cache(&mut self) {
        self.cache_valid = false;
    }
    
    /// 获取缓存的行（惰性计算）
    fn get_rendered_lines(&mut self) -> &[RenderedMessageLine] {
        if !self.cache_valid {
            self.cached_message_lines = self.render_all_messages();
            self.cache_valid = true;
        }
        &self.cached_message_lines
    }
    
    /// 重新渲染所有消息（只在缓存失效时调用）
    fn render_all_messages(&self) -> Vec<RenderedMessageLine> {
        self.messages.iter()
            .map(|msg| self.render_message(msg))
            .collect()
    }
}
```

第一版先采用**整表缓存**，先不引入 `dirty_messages` 细粒度失效。当前阶段优先保证正确性和简单性。

#### 缓存失效时机

| 操作 | 是否失效缓存 |
|------|------------|
| 新增消息 | 是 |
| 删除消息 | 是 |
| 折叠/展开消息 | 是 |
| 切换主题 | 是 |
| 窗口resize | 是 |
| 滚动 | 否 |

#### 缓存依赖

缓存结果至少需要对以下条件敏感：

1. 消息内容
2. `collapsed` 状态
3. 当前主题
4. 消息区域宽度

---

### Phase 3：可见行裁剪（P3）

#### 核心思路

当前实现更适合先对 `RenderedMessageLine` 做可见行裁剪，而不是直接上完整消息级虚拟化。

原因：

1. 当前消息已经会被拍平成 `RenderedMessageLine`
2. 消息包含标题行、正文多行、折叠预览，天然不是固定高度消息块
3. 先做 line-level slicing 就能拿到大部分收益，复杂度更低

```rust
// tui/render/message.rs
pub fn render_messages(frame: &mut Frame, app: &mut App, area: Rect) {
    let lines = app.get_rendered_lines();
    let total_height = area.height as usize;
    // 计算可见范围
    let visible_start = app.scroll_offset;
    let visible_count = total_height;
    let visible_end = (visible_start + visible_count).min(lines.len());
    
    // 只渲染可见部分
    for (idx, line) in lines[visible_start..visible_end].iter().enumerate() {
        let y = area.y + idx as u16;
        frame.render_widget(line.to_widget(), Rect::new(area.x, y, area.width, 1));
    }
    
    // 渲染滚动条
    if lines.len() > visible_count {
        render_scrollbar(frame, app, area);
    }
}
```

#### 优势

- **消息少时**：全部渲染，无感知
- **消息多时**（如1000条）：只渲染可见的30-40行，性能提升明显
- 滚动时只需偏移坐标，不需要重新计算内容

---

### Phase 4：输入框缓存（P4）

#### 核心思路

输入文本和宽度不变时，直接复用上次计算的高度与输入行结果。

```rust
// tui/app.rs
pub struct App {
    // 现有字段...
    
    // 新增：输入框高度缓存
    cached_input_height: u16,
    cached_input_lines: Vec<String>,
    last_input_text: String,
    last_input_width: usize,
}

impl App {
    fn recalculate_input_height(&mut self, width: usize) {
        // 只有输入文本或宽度变化时才重新计算
        if self.input != self.last_input_text || width != self.last_input_width {
            let (lines, _, _) = layout_input_lines(&self.input, width);
            self.cached_input_lines = lines.clone();
            self.cached_input_height = (lines.len() as u16 + 2).clamp(3, 8);
            self.last_input_text = self.input.clone();
            self.last_input_width = width;
        }
    }
    
    fn get_input_height(&mut self, width: usize) -> u16 {
        self.recalculate_input_height(width);
        self.cached_input_height
    }
}
```

---

### Phase 5：减少不必要的重绘（P5）

#### 核心思路

空闲时走事件驱动重绘，忙碌时走低频刷新，避免无意义重绘。

```rust
// tui/mod.rs
fn run_tui() -> Result<()> {
    // ...
    let mut last_draw = Instant::now();
    let mut needs_redraw = true;
    
    while !app.should_quit {
        let busy_refresh = app.is_busy() && last_draw.elapsed() > Duration::from_millis(80);
        if (needs_redraw || busy_refresh) && last_draw.elapsed() > Duration::from_millis(16) {
            terminal.draw(|frame| ui(frame, &mut app))?;
            last_draw = Instant::now();
            needs_redraw = false;
        }
        
        // 事件轮询（非阻塞）
        if event::poll(Duration::from_millis(50))? {
            match event::read()? {
                Event::Key(key) => {
                    app.handle_key_event(key);
                    needs_redraw = true; // 按键后需要重绘
                }
                Event::Mouse(mouse) => {
                    app.handle_mouse_event(mouse);
                    needs_redraw = true;
                }
                Event::Paste(text) => {
                    app.handle_paste(text);
                    needs_redraw = true;
                }
                _ => {}
            }
        }
    }
}
```

#### 重绘触发源

需要触发 redraw 的场景包括：

1. 键盘、鼠标、粘贴事件
2. 异步任务结果到达
3. spinner 状态变化
4. `processing` / `busy_message` 改变
5. 终端尺寸变化

---

### Phase 6：命令系统拆分（P6）

当前 `/plugin`、`/checkpoint`、`/config`、`/memory`、`/wiki`、`/connect` 等命令仍集中在同一实现体中，后续建议拆为：

```text
tui/commands/
├── mod.rs
├── plugin.rs
├── checkpoint.rs
├── config.rs
├── memory.rs
├── wiki.rs
└── connect.rs
```

这一阶段优先级低于状态收敛与渲染性能优化，但对长期维护收益很高。

---

## 实施计划

### Week 1：状态收敛

- [ ] 新增 `state.rs`，抽出 `QueueState`
- [ ] 新增 `interaction.rs`，收敛 pending question / approval / todo confirm
- [ ] 新增 `task_runtime.rs`，统一队列启动、取消、续跑、收尾
- [ ] 保持现有外部接口不变
- [ ] 验证编译通过

### Week 2：文件拆分 + 渲染模块化

- [ ] 创建新目录结构
- [ ] 提取 `theme.rs`
- [ ] 提取 `event.rs`
- [ ] 提取 `render.rs` 和 `render/` 子模块
- [ ] 提取 `app.rs` 聚合入口
- [ ] 验证编译通过

### Week 3：消息缓存 + 可见行裁剪

- [ ] 实现 `cached_message_lines`
- [ ] 主题切换和宽度变化时正确失效缓存
- [ ] 实现可见行裁剪渲染
- [ ] 测试消息多时的性能

### Week 4：输入优化 + 减少重绘

- [ ] 实现输入框高度缓存
- [ ] 实现输入行缓存
- [ ] 实现空闲事件驱动 + 忙碌低频刷新
- [ ] 测试整体流畅度

### Week 5：命令拆分 + Polish

- [ ] 视情况拆分 `commands/` 子模块
- [ ] 性能测试（100/500/1000条消息）
- [ ] 修复边界情况
- [ ] 更新文档

---

## 预期收益

| 指标 | 现状 | 目标 | 提升 |
|------|------|------|------|
| 文件大小 | 8347 行 | 每文件 < 500 行 | 维护性提升 10x |
| 编译时间 | ~30s（全量） | 明显改善的增量编译体验 | 局部改动更聚焦 |
| 消息渲染 | O(n) | O(visible lines) | 消息多时提升明显 |
| 输入响应 | 有延迟 | 实时 | 体验提升明显 |
| 滚动帧率 | < 15fps | 明显提升 | 流畅度提升明显 |

---

## 风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 拆分引入bug | 高 | 逐步拆分，每步验证编译 |
| 状态拆分后事件行为漂移 | 高 | 先收敛状态边界，再迁移事件处理 |
| 可见行裁剪导致滚动边界错误 | 中 | 单独测试滚动到底、折叠消息、长消息 |
| 缓存失效遗漏 | 中 | 完善缓存失效逻辑，单元测试 |
| 主题切换后缓存未失效 | 低 | 主题切换时强制失效缓存 |

---

## 相关文件

- `/workspace/interfaces/cli/src/tui/mod.rs` — 当前主文件（8347行）
- `/workspace/interfaces/cli/src/tui/input.rs` — 输入框逻辑（已拆分）

## 备注

- 拆分时保持向后兼容，不要改变任何外部接口
- 优先保证状态与交互正确，再优化性能
- 每完成一个Phase就做一次回归测试
