/**
 * SaCode Task Protocol — re-export from shared client-core (M1).
 * VSCode/Desktop share one implementation; keep this module as the local import path.
 */
export * from '@cherishron/sacode-client-core';
