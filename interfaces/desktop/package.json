{
  "name": "@cherishron/sacode-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "SaCode Desktop MVP shell (client-core + sacode serve sidecar)",
  "license": "MulanPSL-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "sidecar": "node scripts/desktop-sidecar.mjs --workdir ../..",
    "test": "node --import tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "@cherishron/sacode-client-core": "file:../client-core"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
